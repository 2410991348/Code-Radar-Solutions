#include <stdio.h>

int main() {
    float num;

    // Take floating-point input using scanf
    scanf("%f", &num);

    // Print the entered floating-point number using printf
    printf("You entered: %.2f\n", num);

    return 0;
}