#include <stdio.h>

int main() {
    char ch;

    // Take a single character input using scanf
    scanf("%c", &ch);

    // Print the entered character using printf
    printf("You entered: %c\n", ch);

    return 0;
}