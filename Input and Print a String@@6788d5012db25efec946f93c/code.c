#include <stdio.h>

int main() {
    char str[100];

    // Take a single word (string) input using scanf
    scanf("%s", str);

    // Print the entered string using printf
    printf("You entered: %s\n", str);

    return 0;
}