#include <stdio.h>

int main() {
    // Declare two floating-point variables
    float num1, num2;

    // Take input from the user
    scanf("%f %f", &num1, &num2);

    // Print the product of the two numbers
    printf("Product: %.2f\n", num1 * num2);

    return 0;
}