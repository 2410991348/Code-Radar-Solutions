#include <stdio.h>

int main() {
    int num;

    // Take integer input using scanf
    scanf("%d", &num);

    // Print the entered integer using printf
    printf("You entered: %d\n", num);

    return 0;
}