#include <stdio.h>

int main() {
    char ch;

    // Take a single character input using scanf
    scanf("%c", &ch);

    // Print the ASCII value of the entered character using printf
    printf("ASCII Value: %d\n", ch);

    return 0;
}