#include <stdio.h>

int main() {
    int num1, num2;

    // Take two integers as input using scanf
    scanf("%d %d", &num1, &num2);

    // Print the sum of the two integers using printf
    printf("Sum: %d\n", num1 + num2);

    return 0;
}